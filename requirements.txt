# Python 3.11 (Conda environment: cv_yolo)

# YOLO-World and camera inference
ultralytics==8.4.107
torch==2.13.0
torchvision==0.28.0
opencv-python==5.0.0.93
cv2-enumerate-cameras>=1.3.3
numpy>=2.4.6

# Text encoder used by YOLO-World custom prompts
git+https://github.com/ultralytics/CLIP.git

# On-device speech-to-text
openai-whisper==20250625

# Microphone capture and WAV I/O
sounddevice==0.5.5
soundfile==0.13.1

# Device-setup interface
textual>=8.2.8
rich>=15.0.0

# Offline voice notification
pyttsx3==2.99

# macOS native audio APIs and Apple-Silicon source separation
pyobjc-framework-AVFoundation; sys_platform == "darwin"
pyobjc-framework-SoundAnalysis; sys_platform == "darwin"
mlx-audio==0.4.6; sys_platform == "darwin" and platform_machine == "arm64"

# Formatter
black==26.5.1
